import { EmptyMatcher } from "./empty-matcher";
export declare class ContainerMatcher<ContainerType extends {
    indexOf(content: ContentType): number;
}, ContentType> extends EmptyMatcher<ContainerType> {
    toContain(expectedContent: ContentType): void;
}
